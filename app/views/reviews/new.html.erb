<%= form_for [@restaurant, @review] do |f| %>
  <p>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </p>
  <p>
    <%= f.label :rating %>
    <%= f.select :rating, (1..5) %>
  </p>
  
  <p>
    <%= f.submit %>
  </p>
<% end %>
